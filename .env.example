# Request a key at https://secure.fantasypros.com/api-keys/request/
#
# Only needed to record fixtures (composer test:record, composer fixtures:refresh).
# The default suite (composer test) replays committed fixtures and never reads this.
FANTASYPROS_API_KEY=
